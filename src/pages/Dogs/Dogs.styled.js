import styled from 'styled-components'

export const Wrapper = styled.div`
    width: 100%;
    min-height: calc(100vh - 50px);
    background:var(--bg);
    padding-bottom: 20px;
    overflow: hidden;
`